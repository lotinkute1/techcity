import React from 'react';
import PropTypes from 'prop-types';

ChatBox.propTypes = {
    
};

function ChatBox(props) {
    return (
        <div>
            
        </div>
    );
}

export default ChatBox;